RSpec.describe Log::Current::User do
  it "has a version number" do
    expect(Log::Current::User::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
